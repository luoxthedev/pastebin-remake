<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Paste</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <script>
        function copyToClipboard() {
            const pasteUrl = window.location.href;
            navigator.clipboard.writeText(pasteUrl).then(() => {
                alert("Paste link copied to clipboard!");
            });
        }
    </script>
</head>
<body class="bg-gray-100">
    <div class="container mx-auto p-8">
        <h1 class="text-3xl font-bold text-center mb-8">Your Paste</h1>
        <div class="max-w-lg mx-auto bg-white p-6 rounded shadow-md">
            <pre class="whitespace-pre-wrap"><%= content %></pre>
        </div>
        <div class="text-center mt-4">
            <button onclick="copyToClipboard()" class="bg-green-500 text-white p-3 rounded">Share Paste Link</button>
        </div>
        <div class="text-center mt-4">
            <a href="/" class="text-blue-500">Create another paste</a>
        </div>
    </div>
</body>
</html>
